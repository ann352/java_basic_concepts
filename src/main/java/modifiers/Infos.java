package modifiers;

//modifiers appear before fields, methods, or the class itself
//(no modifier) default (package-private) - access only to classes in the same package
//public - all classes / private - only within this class / protected - access in the same package + inheriting classes
//class - can have pnly default, public access / inner class, field, method, arguments - all modifiers

//to prevent entering inappropriate values into the constructor, we can add an appropriate 'if' to it,
//we can also make fields private and provide appropriate methods to change and retrieve their values, getters and setters, in which we will protect of entering incorrect data

public class Infos {
}
