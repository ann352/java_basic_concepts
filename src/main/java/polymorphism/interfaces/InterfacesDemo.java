package polymorphism.interfaces;

public class InterfacesDemo {
    //one class can implement many interfaces
    //methods in the interface are public abstract but also could be implemented -> 'default' modifier
    //only PUBLIC STATIC fields and no private methods are allowed in interface -> example: LaundryType DEFAULT_LAUNDRY_TYPE = LaundryType.MIXED;
    //thanks to interfaces we can use polimorphism





}
